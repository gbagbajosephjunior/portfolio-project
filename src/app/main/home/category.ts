export interface Icategory{
        id?: string;
        title: string;
        description?: string;
        price?: string;
        categoryId?: string;
        techStack?: string;
        categoryName?: string;
    
}